name: Release Docker Image

on:
  workflow_call:
    inputs:
      runner:
        required: true
        type: string
      registry_url:
        required: true
        type: string
        default: ${{ vars.REGISTRY_URL }}
      registry_username:
        required: true
        type: string
        default: ${{ secrets.REGISTRY_USERNAME }}
      registry_password:
        required: true
        type: string
        default: ${{ secrets.REGISTRY_PASSWORD }}
      image-name:
        required: true
        type: string
        default: ${{ github.repository }}
      dockerfile:
        required: true
        type: string
        default: ./Dockerfile
      context:
        required: true
        type: string
        default: .

jobs:
  build:
    runs-on: ${{ inputs.runner }}
    permissions:
      packages: write
      contents: read
      attestations: write
      id-token: write
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Log in to ${{ inputs.registry_url }}
        uses: docker/login-action@f4ef78c080cd8ba55a85445d5b36e214a81df20a
        with:
          registry: ${{ inputs.registry_url }}
          username: ${{ inputs.registry_username }}
          password: ${{ inputs.registry_password }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@dbef88086f6cef02e264edb7dbf63250c17cef6c # v5.5.0
        with:
          images: ${{ inputs.registry_url }}/${{ inputs.image-name }}
          tags: |
            type=semver,pattern={{version}}

      - name: Build and push Docker image
        id: push
        uses: docker/build-push-action@3b5e8027fcad23fda98b2e3ac259d8d67585f671
        with:
          context: ${{ inputs.context }}
          file: ${{ inputs.dockerfile }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: Generate artifact attestation
        uses: actions/attest-build-provenance@v1
        with:
          subject-name: ${{ inputs.registry_url }}/${{ inputs.image-name }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true
